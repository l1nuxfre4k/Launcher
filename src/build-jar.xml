<project name="Launcher" default="dist">
	<property name="MyApp" value="Launcher" />
	<!-- Create binary distribution -->
	<target name="dist">
		<delete file="${MyApp}.jar"/>
		<jar jarfile="${MyApp}.jar"
         basedir="bin"
         manifest="${MyApp}.mf" />
	</target>
</project>
